import turtle

screen = turtle.Screen()
screen.setup(width=800,height=600)
screen.bgcolor("lightblue")

t = turtle.Turtle()
t.pencolor("darkblue")
t.speed(4)
t.pensize(2)

sides = 100
length = 100
angle = 360/sides

for i in range(sides):
    t.forward(length)
    t.left(angle)

turtle.done
